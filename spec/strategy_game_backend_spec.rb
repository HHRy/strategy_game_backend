# frozen_string_literal: true

RSpec.describe StrategyGameBackend do
  it "has a version number" do
    expect(StrategyGameBackend::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
